#! python


def build(ctx):

    ctx(
        features='run_py_script',
        source='Japan_SCM.py',
        deps=[
            ctx.path_to(ctx, 'IN_DATA', 'Dataset_TermPaper.xlsx'),
        ],
        target=ctx.path_to(ctx, 'OUT_FIGURES', 'Synthetic_Control_Method.png')
    )

